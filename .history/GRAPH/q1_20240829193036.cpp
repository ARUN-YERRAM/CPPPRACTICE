#include<bits/stdc++.h>
using namespace std;

int main(){
    int n;
    cin>>n;
    vector<string>vec;
    for(int i=0;i<n;i++){
        string str;
        cin>>str;
        vec.push_back(str);
    }

    for(int i=0;i<n;i++){
        string st = vec[i];
        sort(st.begin(),st.end());
        for(int j=i+1;j<n;j++){
            if(vec[j] == )
        }
    }
}