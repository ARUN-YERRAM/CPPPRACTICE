#include<bits/stdc++.h>
using namespace std;

int main(){
    string s;
    cin>>s;

    int i = 0;
    int c = 1;
    while(i+1 < s.size()){
        if(s[i] == s[i+1]){
            c++;
        }else{
            if(c >= 7)
        }
    }
}